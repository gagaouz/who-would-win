# 2.0 implementation checkpoint — September 26, 2026

2.0 is not yet in TestFlight. Work is isolated on `develop/2.0` in the dedicated v2 worktree. The original `release/1.1.7` checkout's NUL-delimited Git status still matches its isolation snapshot; its existing unfinished work has not been changed.

- Retro components and reachable home, picker, arena, collection, commerce, parent/settings, tournament, result, and share screens are implemented. iPhone and iPad captures have been reviewed; a few visual corrections await the final integrated build.
- Shared SpriteKit solo/team/tournament playback uses accepted results, appropriate movement, pause/resume/cancellation, reduced motion, and exactly-once reward guards. Quick tournament behavior remains intact. Independent battle/UI diff audits found no removed reachable reward, action, spend, preference, or mode routes.
- All 143 catalog creatures have bundled retro artwork. Fifteen problematic crops were replaced, including swordfish anatomy. Twelve additional local avatar bases support typed custom characters. The 20-atlas validator passes coverage, bounds, transparency, nonblank crops, and absence of opaque overlap between neighboring crop rectangles. The retro app icon is opaque and 1024 square.
- The owner selected local custom avatars without recurring image-service charges. Name-aware forms and stable fantasy avatars render on device, preserving custom IDs and existing play. See [CUSTOM_AVATARS.md](CUSTOM_AVATARS.md). The unreleased paid-service prototype was removed before live API calls or deployment; backend code and dependencies are unchanged.
- The complete-catalog native suite passed 34 unit tests and 7 iPhone UI tests. The same binary passed 6 iPad UI tests. These include actual solo/4v4 play, background/resume/rematch, offline fallback, and broad screen/export captures. They predate final crop/UI/local-avatar changes, so a final run remains required.
- The unchanged backend passes all 46 existing tests, including custom classification/tracking, battle compatibility, story safety, deterministic outcomes, and catalog parity. No production API calls were needed for these tests.
- A disposable simulator upgrade is in progress using synthetic data encoded by actual 1.1.7 model sources. The old build now successfully saves/verifies the synthetic parental PIN using ad-hoc simulator signing. No owner's device data is used.
- Read-only release inspection found 1.1.7/build 109 valid, internal group `self` with all-build access, and production deployment triggers restricted to `main`. A fresh complete ASC history check allocated version 2.0/build 110 at 16:00Z. The candidate was committed at `7b03a2a` and pushed only to `develop/2.0`. No production deployment or TestFlight upload has occurred at this checkpoint.
- No physical iPhone/iPad is attached. Physical upgrade/receipts, iCloud, real permissions, and device performance must be tested by the owner through the internal TestFlight build. These checks must remain explicitly pending rather than falsely marked passed; they block broader release, not creation of the requested owner beta.

Remaining critical path: validate final local avatars and corrected UI; finish the actual in-place simulator upgrade; freeze and commit the candidate; run its final integrated suite; record fresh build allocation; produce and inspect the signed archive; upload to the owner's internal TestFlight group; verify Apple processing and availability of the precise build. Maintain a concrete owner test checklist for capabilities that require a physical device/account.
