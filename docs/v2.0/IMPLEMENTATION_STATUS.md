# 2.0 implementation status — September 26, 2026

**Animal vs Animal 2.0 (110) is available in internal TestFlight.** Apple reports `VALID`, `INTERNAL_ONLY`, and `IN_BETA_TESTING`. The exact build is present in the existing `self` group's build list. What to Test notes were published and read back at 16:36 UTC. See the [final release record](RELEASE_2.0_110.md) and [sanitized Apple evidence](evidence/testflight-2.0-110.json).

- The whole native app uses the retro theme, including home, pickers, arenas, collections, commerce, parental settings, tournaments, results, and share exports. All 143 catalog creatures have bundled retro artwork; the complete 20-atlas validation passed.
- Shared SpriteKit solo/team/tournament animation follows accepted results, with pause/resume, cancellation, reduced motion, stale-response protection, and exactly-once settlement guards. Quick tournament behavior remains intact. Source reviews found no removed reachable reward, action, spend, preference, or mode routes.
- Typed custom fighters receive deterministic avatars made on the device from catalog sprites and twelve additional bases. Recognized words guide appearance; unfamiliar names receive stable fantasy avatars. There is no custom-image server or per-image fee. Existing custom IDs, gameplay, and narration contracts remain intact. See [CUSTOM_AVATARS.md](CUSTOM_AVATARS.md).
- The final native suite passed **41 unit, 9 iPhone UI, and 5 targeted iPad UI test executions**, with zero failures or skips. Final artwork, local avatars, battle screens, tournament dismissal, and rendered share exports were reviewed.
- Both strict in-place synthetic **1.1.7 → 2.0 simulator upgrade** cases passed, preserving pending/settled tournaments, preferences, loaded store state, Documents content, and the synthetic Keychain PIN. The baseline was cold-launched before upgrading; no candidate data repair or reseeding was used.
- The shipping backend source and dependencies are unchanged from the maintenance baseline; all **46 backend tests passed**. No backend deployment or image-provider credential was needed.
- Archived source is `583b5e54a3f6fb6279705dacb50465197318ce67`, published on `develop/2.0`. Native tests ran at `ebcc38e`; only the upgrade harness changed afterward, and all shipping app/project/configuration/test trees have identical Git objects. Signed archive and local internal-only IPA inspection passed before upload.
- `release/1.1.7` remains at `107bf73`; the original checkout's existing dirty Git status still matches its isolation snapshot. Maintenance can continue independently using [BRANCHING.md](BRANCHING.md). Remote production deployment triggers were inspected before publishing only `develop/2.0`.

No physical iPhone/iPad was attached. Actual installation over the owner's App Store copy, receipts/purchases, iCloud/Game Center, permissions, and physical-device performance remain owner acceptance checks. These are explicitly pending, not counted as simulator passes. The [owner checklist](OWNER_TESTFLIGHT_CHECKS.md) is the next step before broader distribution.
